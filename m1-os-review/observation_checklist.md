# M1 셀프체크 — 관찰 체크리스트 (워크시트)

`check_self.sh` 실행 결과를 보고, **정답이 아니라 "무엇을 봤는지"만** 아래 빈칸에 적어보세요.
결과를 그대로 복붙하지 말고, 본인의 말로 한 문장씩 정리하는 것이 목표입니다.

---

## 1. 프로세스 상태

**1-1. STAT 컬럼에서 어떤 상태 문자들이 보였나요?**
(예: S, R, D, Z 등 — 실제로 관찰된 것만)

> 기록:

**1-2. D 상태 프로세스가 있었나요? 있었다면 몇 개, 없었다면 "없음"이라고 적으세요.**

> 기록:

**1-3. 좀비(Z) 프로세스가 있었나요?**

> 기록:

**1-4. PID 1은 어떤 프로세스였나요?** (예: systemd, init, 특정 애플리케이션 등)

> 기록:

**1-5. OOM(Out of Memory) 이력이 있었나요? 없었다면 왜 없다고 판단했나요?** (로그가 비어서 / 권한이 없어서 등)

> 기록:

---

## 2. 파일 권한 / UID

**2-1. `id` 명령 결과, 본인의 UID/GID 숫자는 무엇이었나요?**

> 기록:

**2-2. umask 값은 무엇이었나요?**

> 기록:

**2-3. `ls -ln` 결과에서 파일 소유자가 이름이 아니라 숫자로 나온 것을 확인했나요? 어떤 숫자였나요?**

> 기록:

**2-4. 특수 비트(setuid 등)가 걸린 파일을 찾았나요? 몇 개, 어떤 경로였나요? (없었다면 "없음/권한부족")**

> 기록:

---

## 3. systemd / journalctl

**3-1. `systemctl --failed` 결과, failed 상태 유닛이 있었나요?**

> 기록:

**3-2. `journalctl -n 3` 마지막 3줄에 어떤 종류의 로그가 보였나요?** (내용을 그대로 옮기지 말고 "어떤 종류"인지만)

> 기록:

**3-3. 명령이 실행되지 않은 항목이 있었나요? (예: systemctl/journalctl/dmesg 없음) 어떤 환경이라 그렇다고 판단했나요?**

> 기록:

---

## 종합 한 문장

오늘 셀프체크에서 **가장 인상적이었던 관찰 1개**를, "무엇을 보고 무엇을 판단했는지" 한 문장으로 적어보세요. (내부 시스템명·회사명은 적지 마세요.)

> 기록:
